# Runtime deps (mirror of pyproject.toml; regenerate exact pins via `pip freeze` after install).
# Core harness — LangChain / LangGraph v1
langchain>=1.0,<2
langchain-core>=1.0,<2
langgraph>=1.0,<2
langgraph-checkpoint-sqlite>=2.0
aiosqlite>=0.20
# Model providers
langchain-anthropic>=1.0
langchain-openai>=1.0
langchain-google-genai>=3.0
langchain-qwq>=0.2
# Library search
rank-bm25>=0.2.2
# Tool support / config / CLI
Pillow>=10.4
jinja2>=3.1
pyyaml>=6.0
pydantic>=2.7
python-dotenv>=1.0
typer>=0.12
rich>=13.7
# Dev / tests
pytest>=8.2
pytest-asyncio>=0.24
pytest-mock>=3.14
